{% comment %}
  his-wedding-intro
  --------------------------------------------------------------
  HIS WEDDING紹介ページ（新規の独立ページ、about us的な位置づけ）。
  TOPページに既にある「撮影地(抜粋)」「ご利用の流れ」「よくある質問
  (抜粋)」「おすすめプラン」とは重複させず、TOPには無い
  「差別化ポイント（なぜ選ばれるか）・実績・お客様の声」に絞った構成。
  既存のdress-lp/custom-collection-lpと同じ作法（Tailwind CDN読み込み、
  テーマのフォント変数連動）を踏襲。

  【使い方】
  1. 管理画面→オンラインストア→ページ→新しいページを作成
     （URL/ハンドルは任意。例: about, concept など）
  2. 右側の「テーマテンプレート」で「page.about」を選択
  3. テーマエディタでこのセクションのブロック内容（達成実績の数字、
     お客様の声など）を実際の値に差し替える
  --------------------------------------------------------------
  【要差し替え】
    - 実績人数（stat_number）：仮の数字を入れているので実数に変更
    - お客様の声（testimonialブロック）：仮の文章なので実際の声に差し替え
{% endcomment %}

<script>
  if (!document.getElementById('tw-config')) {
    var twConfig = document.createElement('script');
    twConfig.id = 'tw-config';
    twConfig.innerHTML = "tailwind.config = { theme: { extend: { colors: { ivory: '#FAF8F5', gold: '#B8860B', 'gold-light': '#D4A843', dark: '#1a1a1a' }, fontFamily: { serif: ['var(--font-heading-family, serif)', 'serif'], sans: ['var(--font-body-family, sans-serif)', 'sans-serif'] } } } };";
    document.head.appendChild(twConfig);
  }
  if (!document.getElementById('tw-cdn')) {
    var twScript = document.createElement('script');
    twScript.id = 'tw-cdn';
    twScript.src = '{{ "tailwindcss-3.4.17.js" | asset_url }}';
    document.head.appendChild(twScript);
  }
</script>

<style>
  #his-wedding-intro-{{ section.id }} {
    font-family: var(--font-body-family, sans-serif);
    font-size: calc(var(--font-body-scale, 1) * 100%);
    color: #333333;
  }
  #his-wedding-intro-{{ section.id }} .font-serif { font-family: var(--font-heading-family, serif); }
  #his-wedding-intro-{{ section.id }} .divider-gold { width: 48px; height: 2px; background: #B8860B; margin: 16px auto; }
</style>

<div id="his-wedding-intro-{{ section.id }}" class="w-full bg-white">

  {% comment %} --- 1. ヒーロー --- {% endcomment %}
  {%- assign hero_image_position_style = 'object-position: ' | append: section.settings.hero_image_position | append: ';' -%}
  <section class="relative w-full flex flex-col justify-center items-center text-center" style="height: 60vh;">
    {% if section.settings.hero_image != blank %}
      {{ section.settings.hero_image | image_url: width: 2000 | image_tag: class: 'absolute inset-0 w-full h-full object-cover', alt: section.settings.hero_title, style: hero_image_position_style }}
    {% else %}
      <div class="absolute inset-0 w-full h-full bg-gray-200"></div>
    {% endif %}
    <div class="absolute inset-0" style="background: linear-gradient(to top, rgba(0,0,0,0.75), rgba(0,0,0,0.35) 55%, rgba(0,0,0,0.15));"></div>
    <div class="relative z-10 px-6 w-full max-w-3xl">
      <p class="font-serif tracking-widest uppercase mb-4 text-sm" style="color:{{ section.settings.hero_eyebrow_color | default: '#ffffff' }};">{{ section.settings.hero_eyebrow }}</p>
      <h1 class="font-serif font-light leading-tight mb-4" style="font-size: calc(var(--font-heading-scale, 1) * 40px); color:{{ section.settings.hero_title_color | default: '#ffffff' }};">{{ section.settings.hero_title | newline_to_br }}</h1>
      <p class="font-light" style="font-size: calc(var(--font-body-scale, 1) * 16px); color:{{ section.settings.hero_lede_color | default: '#ffffff' }};">{{ section.settings.hero_lede }}</p>
    </div>
  </section>

  {% comment %} --- 2. 選ばれる理由（特徴3つ） --- {% endcomment %}
  <section class="py-16 md:py-24">
    <div class="max-w-6xl mx-auto px-6">
      <div class="text-center mb-16">
        <p class="font-serif tracking-widest uppercase mb-2 text-sm" style="color:#B8860B;">{{ section.settings.features_eyebrow | default: 'WHY HIS WEDDING' }}</p>
        <h2 class="font-medium" style="font-size: calc(var(--font-heading-scale, 1) * 28px);">{{ section.settings.features_title | default: 'HIS WEDDINGが選ばれる理由' }}</h2>
        <div class="divider-gold"></div>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-10 text-center">
        {% assign feature_blocks = section.blocks | where: 'type', 'feature' %}
        {% for block in feature_blocks %}
          <div {{ block.shopify_attributes }}>
            <div class="w-16 h-16 mx-auto mb-6 rounded-full flex items-center justify-center" style="background-color:#FAF8F5;color:#B8860B;">
              <span class="font-serif text-xl">{{ forloop.index }}</span>
            </div>
            <h3 class="font-bold mb-3" style="font-size: calc(var(--font-heading-scale, 1) * 18px);">{{ block.settings.title }}</h3>
            <p class="text-gray-600 leading-relaxed text-sm">{{ block.settings.desc }}</p>
          </div>
        {% endfor %}
      </div>
    </div>
  </section>

  {% comment %} --- 3. 実績バナー --- {% endcomment %}
  <section class="py-14 text-center text-white" style="background-color:#1a1a1a;">
    <div class="max-w-3xl mx-auto px-6">
      <p class="font-serif tracking-widest uppercase mb-3 text-xs" style="color:#D4A843;">{{ section.settings.stat_eyebrow | default: 'TRACK RECORD' }}</p>
      <p class="font-serif" style="font-size: calc(var(--font-heading-scale, 1) * 44px);">
        {{ section.settings.stat_number | default: '500' }}<span style="font-size:0.5em;">{{ section.settings.stat_suffix | default: '組以上' }}</span>
      </p>
      <p class="text-sm text-gray-300 mt-2">{{ section.settings.stat_label | default: 'のカップルにご利用いただいています（要実数差し替え）' }}</p>
    </div>
  </section>

  {% comment %} --- 4. 先輩カップルの声 --- {% endcomment %}
  {% assign testimonial_blocks = section.blocks | where: 'type', 'testimonial' %}
  {% if testimonial_blocks.size > 0 %}
  <section class="py-16 md:py-24" style="background-color:#FAF8F5;">
    <div class="max-w-6xl mx-auto px-6">
      <div class="text-center mb-16">
        <p class="font-serif tracking-widest uppercase mb-2 text-sm" style="color:#B8860B;">{{ section.settings.testimonial_eyebrow | default: 'VOICE' }}</p>
        <h2 class="font-medium" style="font-size: calc(var(--font-heading-scale, 1) * 28px);">{{ section.settings.testimonial_title | default: '先輩カップルの声' }}</h2>
        <div class="divider-gold"></div>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        {% for block in testimonial_blocks %}
          <div class="bg-white rounded-lg overflow-hidden shadow-sm" {{ block.shopify_attributes }}>
            {% if block.settings.image != blank %}
              <div class="w-full relative" style="padding-top:75%;">
                {{ block.settings.image | image_url: width: 700 | image_tag: class: 'absolute top-0 left-0 w-full h-full object-cover', loading: 'lazy', alt: '' }}
              </div>
            {% endif %}
            <p class="text-sm text-gray-700 leading-relaxed p-8">{{ block.settings.quote }}</p>
          </div>
        {% endfor %}
      </div>
    </div>
  </section>
  {% endif %}

  {% comment %}
    --- 5. 最後のCTA（2枚。詳しい撮影地一覧・プラン一覧・FAQ等は
    それぞれ専用ページやTOPページに既にあるため、ここでは深追いせず
    「その先」への導線だけを置く）
  {% endcomment %}
  <section class="py-16 md:py-24">
    <div class="max-w-5xl mx-auto px-6 grid grid-cols-1 md:grid-cols-2 gap-6">
      {% assign cta_blocks = section.blocks | where: 'type', 'cta_card' %}
      {% for block in cta_blocks %}
        {%- assign cta_overlay_top = block.settings.overlay_opacity | default: 70 | times: 0.01 -%}
        {%- assign cta_overlay_bottom = cta_overlay_top | times: 0.14 -%}
        <div class="relative rounded-lg overflow-hidden" style="aspect-ratio: 4/3;" {{ block.shopify_attributes }}>
          {% if block.settings.image != blank %}
            {{ block.settings.image | image_url: width: 900 | image_tag: class: 'absolute inset-0 w-full h-full object-cover', alt: block.settings.title }}
          {% else %}
            <div class="absolute inset-0 bg-gray-200"></div>
          {% endif %}
          <div class="absolute inset-0" style="background:linear-gradient(to top, rgba(0,0,0,{{ cta_overlay_top }}), rgba(0,0,0,{{ cta_overlay_bottom }}));"></div>
          <div class="absolute bottom-0 left-0 right-0 p-6">
            <h3 class="font-serif mb-3" style="font-size: calc(var(--font-heading-scale, 1) * 22px); color:{{ block.settings.title_color | default: '#ffffff' }};">{{ block.settings.title }}</h3>
            {% if block.settings.button_text != blank %}
              <a href="{{ block.settings.button_url }}" class="inline-block px-6 py-2.5 rounded-full text-xs font-medium transition hover:opacity-80" style="background-color:{{ block.settings.button_bg_color | default: '#B8860B' }};color:{{ block.settings.button_text_color | default: '#ffffff' }};">{{ block.settings.button_text }}</a>
            {% endif %}
          </div>
        </div>
      {% endfor %}
    </div>
  </section>

</div>

{% schema %}
{
  "name": "HIS WEDDING紹介ページ",
  "settings": [
    { "type": "image_picker", "id": "hero_image", "label": "ヒーロー背景画像" },
    { "type": "select", "id": "hero_image_position", "label": "ヒーロー画像の位置", "options": [
        { "value": "center", "label": "中央" },
        { "value": "top", "label": "上部" },
        { "value": "bottom", "label": "下部" },
        { "value": "left", "label": "左寄せ" },
        { "value": "right", "label": "右寄せ" }
      ], "default": "center" },
    { "type": "text", "id": "hero_eyebrow", "label": "ヒーロー英字見出し", "default": "ABOUT HIS WEDDING" },
    { "type": "color", "id": "hero_eyebrow_color", "label": "英字見出しの色", "default": "#ffffff" },
    { "type": "textarea", "id": "hero_title", "label": "ヒーロー メインタイトル（改行可）", "default": "海外フォトウェディングを、\nもっと身近に。" },
    { "type": "color", "id": "hero_title_color", "label": "メインタイトルの色", "default": "#ffffff" },
    { "type": "textarea", "id": "hero_lede", "label": "ヒーロー リード文", "default": "国内で衣裳を試着して、海外の絶景で撮影するだけ。準備の負担を最小限に、一生に一度の1枚を残すためのフォトウェディングサービスです。" },
    { "type": "color", "id": "hero_lede_color", "label": "リード文の色", "default": "#ffffff" },

    { "type": "header", "content": "実績バナー" },
    { "type": "text", "id": "stat_eyebrow", "label": "英字見出し", "default": "TRACK RECORD" },
    { "type": "text", "id": "stat_number", "label": "実績の数字（要実数差し替え）", "default": "500" },
    { "type": "text", "id": "stat_suffix", "label": "数字の単位", "default": "組以上" },
    { "type": "text", "id": "stat_label", "label": "説明文", "default": "のカップルにご利用いただいています" },

    { "type": "header", "content": "選ばれる理由セクション" },
    { "type": "text", "id": "features_eyebrow", "label": "英字見出し", "default": "WHY HIS WEDDING" },
    { "type": "text", "id": "features_title", "label": "見出し", "default": "HIS WEDDINGが選ばれる理由" },

    { "type": "header", "content": "お客様の声セクション" },
    { "type": "text", "id": "testimonial_eyebrow", "label": "英字見出し", "default": "VOICE" },
    { "type": "text", "id": "testimonial_title", "label": "見出し", "default": "先輩カップルの声" }
  ],
  "blocks": [
    {
      "type": "feature",
      "name": "選ばれる理由（1項目）",
      "settings": [
        { "type": "text", "id": "title", "label": "タイトル", "default": "来店不要、オンラインで完結" },
        { "type": "textarea", "id": "desc", "label": "説明文", "default": "プランのご相談からお申し込みまで、オンラインで完結。忙しいおふたりでも、すきま時間で準備を進められます。" }
      ]
    },
    {
      "type": "testimonial",
      "name": "お客様の声（1件・要差し替え）",
      "settings": [
        { "type": "image_picker", "id": "image", "label": "お客様の写真（任意。空欄なら画像枠自体を表示しない）" },
        { "type": "textarea", "id": "quote", "label": "コメント（仮の文章。実際の声に差し替えてください）", "default": "国内で試着してから現地に持って行けたので、サイズの不安もなく安心して撮影に臨めました。" }
      ]
    },
    {
      "type": "cta_card",
      "name": "最後のCTAカード",
      "settings": [
        { "type": "image_picker", "id": "image", "label": "画像" },
        { "type": "text", "id": "title", "label": "タイトル", "default": "撮影地から選ぶ" },
        { "type": "color", "id": "title_color", "label": "タイトルの色", "default": "#ffffff" },
        { "type": "range", "id": "overlay_opacity", "min": 0, "max": 100, "step": 5, "unit": "%", "label": "画像に重ねる暗さ", "info": "数値を上げるほど画像が暗くなり、文字が読みやすくなります。", "default": 70 },
        { "type": "text", "id": "button_text", "label": "ボタン文言", "default": "撮影地一覧を見る" },
        { "type": "url", "id": "button_url", "label": "ボタンリンク先" },
        { "type": "color", "id": "button_bg_color", "label": "ボタン背景色", "default": "#B8860B" },
        { "type": "color", "id": "button_text_color", "label": "ボタン文字色", "default": "#ffffff" }
      ]
    }
  ],
  "presets": [
    {
      "name": "HIS WEDDING紹介ページ",
      "blocks": [
        { "type": "feature" },
        { "type": "feature", "settings": { "title": "衣裳・撮影・データ、まとめてワンプライス", "desc": "ウェディングドレス＆タキシードのレンタル、現地カメラマンによる撮影、データ納品までを含んだオールインワンプラン。追加費用の心配がありません。" } },
        { "type": "feature", "settings": { "title": "当日は身ひとつでOK", "desc": "衣裳は日本国内の提携サロンで試着したものを事前にお渡し。現地では着替えと撮影に集中するだけです。" } },
        { "type": "testimonial" },
        { "type": "testimonial", "settings": { "quote": "オンラインでのやり取りだけで準備が完結し、仕事をしながらでも無理なく進められました。" } },
        { "type": "testimonial", "settings": { "quote": "当日は撮影に集中するだけで良かったので、旅行自体も存分に楽しめました。" } },
        { "type": "cta_card", "settings": { "title": "撮影地から選ぶ", "button_text": "撮影地一覧を見る" } },
        { "type": "cta_card", "settings": { "title": "衣裳から選ぶ", "button_text": "衣裳一覧を見る" } }
      ]
    }
  ]
}
{% endschema %}
