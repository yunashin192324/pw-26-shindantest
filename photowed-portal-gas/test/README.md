# 動作テスト（開発者向け）

Google Apps Script の実行環境（SpreadsheetApp / MailApp / CacheService など）を
Node.js 上で模擬して、`Code.gs` のロジックを実際に動かして検証するテストです。
スプレッドシートやデプロイは不要で、手元ですぐ実行できます。

```bash
cd photowed-portal-gas/test
node gas_test.js
```

`結果: 244 件成功 / 0 件失敗` と表示されれば正常です。

## 画面（フロントエンド）のテスト

サーバー側のテストだけでは「ボタンが繋がっていない」「保存したら別タブへ飛ぶ」といった
画面側の不具合を拾えません。`ui_test.js` は jsdom 上に実際の `Index.html` を組み立て、
`google.script.run` を `Code.gs` へ繋いで、人が操作するのと同じ経路で検証します。

```bash
cd photowed-portal-gas/test
npm install --no-save jsdom   # 初回のみ
node ui_test.js
```

`画面テスト結果: 37 件成功 / 0 件失敗` と表示されれば正常です。

| # | 項目 |
|---|---|
| U1 | ログイン → 案件一覧 → 詳細を開く |
| U2 | ドライブタブの見出し |
| U3 | 予約内容・現地記入欄タブ下部のボタンで確定でき、保存後もそのタブに留まること |
| U4 | 画面表記がWEDLINKになっていること |
| U5 | 同意書のチェックが保存・解除でき、必須支店では警告が出ること |
| U6 | セール名がマスタ候補＋自由入力で保存できること |
| U7 | スプレッドシート由来の文字列でXSSが起きないこと（実際にスクリプトが動かないことを確認） |
| U8 | 当日表に同意書・セール名が出ること |
| U9 | 未保存の変更がタブをまたいで保持されること |

## 何を検証しているか

| # | 項目 |
|---|---|
| 1 | 既存シートへの列追加（`setupPortal` 再実行で列が増え、既存データが壊れないこと） |
| 2 | やり取り履歴の書き込みが「列名基準」で正しい列に入ること |
| 3 | 納品期限アラート（過去一覧も走査／再通知／打ち切り／支店ごとの日数設定） |
| 4 | 過去一覧への移動で列がずれないこと |
| 5 | 統計ダッシュボードの内訳合計が件数と一致すること |
| 6 | 要対応（未読）判定と、既読にすると元の並び順に戻ること |
| 7 | ステータスの権限ゲート（日本側／支店側の書き分け、CR→CW/CF 制限など） |
| 8 | 支店のデータ分離（他支店の案件が見えない／統計はJP専用） |
| 9 | 新規案件の採番（アーカイブ済みと衝突しない）と通知先の向き |
| 10 | 現地記入欄・請求番号のカスタム名称 |
| 11 | 3択（保存のみ／メッセージのみ／変更＋メッセージ）の挙動 |
| 12 | 撮影45日前アラート |
| 13 | 支店マスタの表記ゆれ耐性（ロール・支店コードの大文字小文字／空白） |
| 14 | `apiSaveBranch` が他の列を消さないこと |
| 15 | 列が無い場合に分かりやすいエラーになること |
| 16 | 撮影日変更時に履歴・通知メールが正しい案件に紐づくこと |
| 17 | 既読チェックの認可（他支店の履歴を操作できない）と新規作成時の支店コード検証 |
| 18 | 未読フラグ方式（送信で立つ／既読で下りる／複数未読の扱い／履歴走査に依存しないこと） |
| 19 | 定期処理の例外処理（1件失敗しても継続）とシステム管理者への障害通知 |
| 20 | 未返信リマインド（既定3日／支店ごとの督促日数／既読で解除／複数案件を1通に集約） |
| 21 | 納品待ち一覧・当日スケジュール表・案件タイムライン（スコープと除外条件を含む） |
| 22 | ダブルブッキング検知（表記ゆれ吸収・支店をまたがない）・請求不備チェック・書き出し・定型文マスタ |
| 23 | 同意書（支店ごとの必須設定・手動更新・フォーム連携・二重送信・タイムライン記録・当日表表示）・セール名 |
| 24 | `setupPortal` を実際に通す（初回セットアップでシード行と列がズレないこと・再実行しても壊れないこと） |
| 25 | 旧バージョンからのマイグレーション（同意書・セール名の列追加で既存データとログインが壊れないこと） |
| 26 | 画面から保存した日付が「日付」として後続処理（過去一覧への移動・45日前アラート・当日表）で使えること |

## 注意

`gas_harness.js` はテスト用の簡易モックであり、Apps Script の完全な再現ではありません
（日付のタイムゾーン処理などは簡略化しています）。ロジックの回帰チェック用途に使ってください。
`Code.gs` を修正したら、`gas_test.js` と `ui_test.js` の両方を実行してから反映することをおすすめします。

なお `gas_harness.js` は Node の `vm` で別realmを作るため、**シートに入れる日付は必ず
`ctx.__newDate` / `ctx.__daysFromToday` か `Utilities.parseDate` 経由で作ってください。**
テストコード側で素の `new Date()` を使うと `instanceof Date` が false になり、
実際には動く／動かないに関わらずテスト結果が信用できなくなります。
